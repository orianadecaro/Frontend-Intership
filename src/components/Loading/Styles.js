import { StyleSheet} from 'react-native';


const styles = StyleSheet.create({
  container: {
    width: 288,
    height: 404,
  },

  loadingtext: {
    color: 'white',
    top: 190,
    textAlign: 'center',
    fontSize: 30
  }
});

export default styles;

